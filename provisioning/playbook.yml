---
- name: Kali GUI via VNC (XFCE + TigerVNC) for Guacamole
  hosts: kali-host
  become: true
  vars:
    vnc_user: kali
    vnc_password: "password"
    vnc_display: "1"
    vnc_geometry: "1920x1080"
    vnc_depth: "24"
    allow_vnc_nonlocal: true

  tasks:
    - name: Install XFCE + TigerVNC
      apt:
        update_cache: true
        name:
          - xfce4
          - xfce4-goodies
          - dbus-x11
          - xauth
          - tigervnc-standalone-server
          - tigervnc-common
        state: present

    - name: Ensure ~/.vnc exists
      file:
        path: "/home/{{ vnc_user }}/.vnc"
        state: directory
        owner: "{{ vnc_user }}"
        group: "{{ vnc_user }}"
        mode: "0700"

    - name: Set VNC password file
      shell: |
        set -euo pipefail
        printf "%s\n" "{{ vnc_password }}" | vncpasswd -f > "/home/{{ vnc_user }}/.vnc/passwd"
        chown "{{ vnc_user }}:{{ vnc_user }}" "/home/{{ vnc_user }}/.vnc/passwd"
        chmod 0600 "/home/{{ vnc_user }}/.vnc/passwd"
      args:
        executable: /bin/bash

    - name: Set xstartup to launch XFCE
      copy:
        dest: "/home/{{ vnc_user }}/.vnc/xstartup"
        owner: "{{ vnc_user }}"
        group: "{{ vnc_user }}"
        mode: "0755"
        content: |
          #!/bin/sh
          unset SESSION_MANAGER
          unset DBUS_SESSION_BUS_ADDRESS
          exec startxfce4

    - name: Install vncserver@.service
      copy:
        dest: /etc/systemd/system/vncserver@.service
        owner: root
        group: root
        mode: "0644"
        content: |
          [Unit]
          Description=TigerVNC server on display :%i
          After=network.target

          [Service]
          Type=forking
          User={{ vnc_user }}
          PAMName=login
          PIDFile=/home/{{ vnc_user }}/.vnc/%H:%i.pid
          ExecStartPre=-/usr/bin/vncserver -kill :%i
          ExecStart=/usr/bin/vncserver :%i -geometry {{ vnc_geometry }} -depth {{ vnc_depth }} {{ "-localhost no" if allow_vnc_nonlocal else "-localhost yes" }}
          ExecStop=/usr/bin/vncserver -kill :%i

          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd
      systemd:
        daemon_reload: true

    - name: Start VNC on :{{ vnc_display }} (TCP 590{{ vnc_display }})
      systemd:
        name: "vncserver@{{ vnc_display }}"
        state: started
        enabled: true
